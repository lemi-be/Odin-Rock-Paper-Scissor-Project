// make welcoming message
// set Function promptMessage
function promptMessage() {
  // dispaly welcome message
  console.log("Welcome to Rock Paper Message game!!");
}

// Asking user to choose
// set function promptUser
function promptMessage() {
  // set variable to store users choice
  let userChoice = prompt("Choose one (Rock, Paper, Scissors)");
  return userChoice;
}

